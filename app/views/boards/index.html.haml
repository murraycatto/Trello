= render '/layouts/header'
.boards-wrapper
  - @teams.each do |team|
    = render team
  .create-new-team-holder.hide-body-clicks-btn{onclick: "$('#create-team-form').show();"}
    .create-new-team-item
      Create a new team...
  #create-team-form.hide-body-clicks{style:"display:none"}
    .card-form-holder
      .card-form-header
        .card-form-title
          Create Team
          .close-button-holder{onclick:"$('#create-team-form').hide();"}
            .fa.fa-times
      = form_with(model: Team.new, url: [ Team.new ],id: "TeamForm") do |f|
        .card-form-body
          .card-form-label
            = f.label :name
          .card-form-input
            = f.text_field :name
          .card-form-label
            = f.label :description
          .card-form-input
            = f.text_area :description
          .card-form-submit
            = f.submit "Create"
      .card-form-line
      .card-form-text
        A team is a group of boards and people. Use it to organize your company, side hustle, family, or friends.
